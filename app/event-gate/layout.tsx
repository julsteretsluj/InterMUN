import { AppleGateLayout } from "@/components/ui/AppleGateLayout";

export default function EventGateLayout({ children }: { children: React.ReactNode }) {
  return <AppleGateLayout>{children}</AppleGateLayout>;
}

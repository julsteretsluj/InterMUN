---
description: InterMUN Apple-inspired UI system and constraints
alwaysApply: true
---

# InterMUN Apple-Inspired UI Rule

Use the Apple-inspired light design system as the source of truth for UI work.

## File / Page Structure
- Keep the 4-page structure in mind for all design decisions:
  - `01 — Foundations`
  - `02 — Components`
  - `03 — Patterns (Screens)`
  - `04 — Prototypes`

## Foundations
- Respect base color intents (light primary):
  - `bg/primary #FFFFFF`, `bg/secondary #F5F5F7`, `bg/tertiary #FBFBFD`
  - `text/primary #1D1D1F`, `text/secondary #424245`, `text/muted #6E6E73`
  - `accent/primary #0071E3`, `accent/bright #2997FF`, `accent/success #34C759`, `accent/danger #FF3B30`
  - Borders: `#D2D2D7`, `rgba(0,0,0,0.08)`
- Dark mode is refined Apple dark (true black / gray materials), secondary to light.
- Respect typography intents:
  - SF Pro / system UI Sans for app chrome (`h1/h2/body`); Inter as fallback
  - Serif for document surfaces
  - Mono for system metadata
- Use 8pt spacing rhythm: `4, 8, 12, 16, 24, 32, 48`.
- Soft shadows only (e.g. `0 8px 30px rgba(0,0,0,.06)`).
- Glass/frosted materials only for sticky chrome — not everywhere.

## Components and Naming
- Keep existing naming and structural semantics intact.
- Build and evolve UI using the specified component families:
  - Sidebar, Top Bar, Card, Dashboard Panel, Live Session Bar, Committees Card
  - Documents (Clause Item / Editor / Toolbar)
  - Messages Thread, Voting Button / Results Bar
  - Chair Controls (Speakers List / Motion Button)
  - Profile Header / Stats, Admin Table

## Patterns
- Compose screens from reusable components (Dashboard, Committees, Documents, Voting, Chair Controls).
- Prefer consistency and reuse over one-off page-specific styling.
- Content max-width around 1200–1400px with generous margins.

## Auto Layout / Layout Behavior
- Apply auto-layout principles in implementation:
  - vertical page flow
  - internal spacing around `16–24`
  - inter-element gaps around `12–16`
- Keep sidebar fixed and main content fill behavior.

## Strict Constraint
- Enhance UI only through:
  - color
  - texture
  - motion
- Do **not** change:
  - labels
  - information architecture / structural naming
